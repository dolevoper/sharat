* Support command line args
    * PORT
    * QUIET
* Support more file types?
* Custom 401
* Module and file replacement instead of full page reload
* Support more than 6 concurrent tabs
* Default tsconfig to improve debugging in browser
